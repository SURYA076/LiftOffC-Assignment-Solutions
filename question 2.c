#include<stdio.h>
int main()
{
    int X,Y;
    scanf("%d%d",&X,&Y);
    if(X>Y)
    {
        printf("%d",X);
    }
    else
    {
        printf("%d",Y);
    }
    return 0;
}#include<stdio.h>
int main(){
int a,b;
scanf("%d %d",&a,&b);
if(a>b){
    printf("The max number is:%d",a);
}
else{
    printf("The max number is:%d",b);
}
}